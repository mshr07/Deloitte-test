apiVersion: v1
kind: Service
metadata:
 name: spring-boot-login-service
spec:
 selector:
   app: spring-boot-login
 type: NodePort
 ports:
   - protocol: TCP
     port: 8080
     targetPort: 8080
     nodePort: 30005
